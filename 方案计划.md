# 个人网站搭建方案 — Astro 基础版

## 项目信息
- **项目名称：** ZONE 的个人网站 (thing-personal-site)
- **项目路径：** D:\Project\thing-personal-site\
- **部署方式：** GitHub Pages → https://AKtzone.github.io/ZONE/
- **技术栈：** Astro + Markdown + GitHub Pages

> 本方案为「基础版」，先让网站跑起来。后续如有需要可升级：可视化后台管理、评论区、搜索等功能。

---

## 一、网站结构

```
thing-personal-site/
├── public/
│   └── favicon.ico
├── src/
│   ├── components/           # 组件
│   │   ├── Header.astro      # 顶部导航栏
│   │   ├── Footer.astro      # 底部信息
│   │   └── ProjectCard.astro # 作品卡片组件
│   ├── content/
│   │   ├── blog/             # 博客文章（.md 文件）
│   │   └── projects/         # 作品数据（.md 文件）
│   ├── layouts/
│   │   └── BaseLayout.astro  # 页面布局模板
│   ├── pages/
│   │   ├── index.astro       # 首页
│   │   ├── blog/
│   │   │   ├── index.astro   # 博客列表页
│   │   │   └── [slug].astro  # 博客详情页
│   │   ├── projects/
│   │   │   ├── index.astro   # 作品列表页
│   │   │   └── [slug].astro  # 作品详情页
│   │   └── about.astro       # 关于我
│   └── styles/
│       └── global.css        # 全局样式
├── .github/workflows/
│   └── deploy.yml            # GitHub Actions 自动部署
├── astro.config.mjs
├── package.json
└── tsconfig.json
```

---

## 二、实施步骤

### 步骤 1：初始化 Astro 项目

在 D:\Project\thing-personal-site\ 下执行：

```bash
npm create astro@latest
```

交互式问答选择：
- Where should we create: `.`（当前目录）
- How would you like to start: `Empty`
- Do you plan to write TypeScript: `Yes`
- Install dependencies: `Yes`
- Initialize Git: `Yes`

### 步骤 2：配置 astro.config.mjs

```javascript
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://AKtzone.github.io',
  base: '/ZONE',
  outDir: './dist',
});
```

> 注意：base 设为 `/ZONE` 是因为你的 Pages 地址是 `https://AKtzone.github.io/ZONE/`。不设置的话静态资源路径会出错。

### 步骤 3：创建组件

#### 3.1 Header.astro
- 导航栏：首页 | 博客 | 作品集 | 关于我
- 当前页面高亮
- 响应式（手机上折叠成汉堡菜单）

#### 3.2 Footer.astro
- 版权信息：© 2026 ZONE
- 社交链接：GitHub 等

#### 3.3 ProjectCard.astro
- 接收作品数据作为 props
- 展示：封面图、标题、简短描述、技术栈标签

### 步骤 4：创建布局模板 BaseLayout.astro

```astro
---
export interface Props {
  title: string;
  description?: string;
}
const { title, description } = Astro.props;
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description || "ZONE 的个人网站"} />
  <link rel="icon" type="image/svg+xml" href="/ZONE/favicon.ico" />
  <link rel="stylesheet" href="/ZONE/src/styles/global.css" />
</head>
<body>
  <Header />
  <main>
    <slot />
  </main>
  <Footer />
</body>
</html>
```

> 注意：所有静态资源路径（css、图片等）前面都要加 `/ZONE/`

### 步骤 5：创建页面

#### 5.1 首页 index.astro
- Hero 区域：头像 + "Hi，我是 ZONE" + 一句话个人介绍
- 精选内容区域：展示最新 3 篇博客 + 最新 3 个作品
- 简洁大气的设计

#### 5.2 博客列表页 blog/index.astro
- 用 `Astro.glob()` 读取 `src/content/blog/*.md` 所有文章
- 卡片列表：标题、发布日期、摘要、标签
- 按日期倒序排列（最新的在最前面）

#### 5.3 博客详情页 blog/[slug].astro
- 动态路由，根据文件名加载对应的 Markdown
- 展示完整文章内容
- 显示发布日期和标签

#### 5.4 作品列表页 projects/index.astro
- 读取 `src/content/projects/*.md`
- 网格卡片布局

#### 5.5 作品详情页 projects/[slug].astro
- 展示详细介绍
- 如果有链接，显示「项目链接」和「GitHub 链接」按钮

#### 5.6 关于我 about.astro
- 个人介绍
- 技能标签列表（HTML、CSS、JavaScript、Python 等）
- 联系方式

### 步骤 6：创建全局样式 global.css

```css
/* 设计风格 */
:root {
  --primary: #2563eb;
  --bg: #ffffff;
  --bg-secondary: #f8fafc;
  --text: #1e293b;
  --text-secondary: #64748b;
  --border: #e2e8f0;
  --radius: 8px;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
body {
  font-family: -apple-system, system-ui, sans-serif;
  color: var(--text);
  background: var(--bg);
  line-height: 1.6;
}
a { color: var(--primary); text-decoration: none; }
a:hover { text-decoration: underline; }
.container { max-width: 960px; margin: 0 auto; padding: 0 20px; }

/* 这里只需要写基础样式，Trae 可以按这个风格补充完整 */
```

### 步骤 7：创建示例内容

#### 示例博客：src/content/blog/hello-world.md

```markdown
---
title: 你好，世界！
date: 2026-05-10
description: 这是我的第一篇博客文章
tags: [生活, 随笔]
---

这是我的个人网站第一篇文章！

终于有了属于自己的小天地，以后会在这里分享一些有趣的东西。

请多关照！😊
```

#### 示例作品：src/content/projects/first-site.md

```markdown
---
title: ZONE 的个人网站
date: 2026-05-10
description: 使用 Astro 搭建的综合性个人主页
tech: [Astro, Markdown, GitHub Pages]
url: https://AKtzone.github.io/ZONE/
github: https://github.com/AKtzone/ZONE
---

这是我的个人网站，使用 Astro 框架搭建，部署在 GitHub Pages 上。

**功能：**
- 博客系统
- 作品展示
- 个人介绍
```

> Markdown 的 `---` 之间的内容叫「Frontmatter」，Astro 会自动读取这些数据来生成页面。

### 步骤 8：配置 GitHub Actions 自动部署

创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

### 步骤 9：本地测试与部署

```bash
# 本地预览
npm run dev
# 浏览器访问 http://localhost:4321/ZONE/

# 构建生产版本
npm run build

# 推送到 GitHub
git add .
git commit -m "初始化个人网站"
git push origin main
```

推送后，GitHub Actions 会自动构建部署，大约 1-2 分钟后访问 https://AKtzone.github.io/ZONE/ 就能看到网站了。

---

## 三、更新内容的方式

网站上线后，日常更新内容很简单：

### 写新博客
1. 在 Trae 里打开 `src/content/blog/` 目录
2. 新建一个 `.md` 文件，比如 `my-new-post.md`
3. 按格式写好 Frontmatter + 正文
4. 保存 → 推送 GitHub → 自动部署

### 添加新作品
1. 在 `src/content/projects/` 下新建 `.md` 文件
2. 按格式填写作品信息
3. 保存 → 推送

### 修改个人介绍
编辑 `src/pages/about.astro` 即可

> 以后如果想加可视化后台（在网页上写文章），可以升级到 Decap CMS 版，这个方案保留做扩展。

---

## 四、设计风格参考

| 项目 | 值 |
|:----|:----|
| 主色调 | `#2563eb`（蓝色）|
| 背景色 | `#ffffff` / `#f8fafc` |
| 文字色 | `#1e293b`（标题）/ `#64748b`（正文）|
| 字体 | 系统无衬线字体 |
| 圆角 | 8px |
| 布局 | 居中定宽，最大 960px |

---

## 五、后续可升级的功能

- [ ] 可视化后台（Decap CMS）
- [ ] 评论区（Giscus，基于 GitHub Issues）
- [ ] 搜索功能（Pagefind）
- [ ] 暗色模式切换
- [ ] 访问统计
- [ ] 自定义域名
- [ ] RSS 订阅

---

## 六、常见问题

**Q：本地预览时图片/样式加载不出来？**
A：检查路径是否加了 `/ZONE/` 前缀

**Q：推送后网站没变化？**
A：检查 GitHub Actions 是否运行成功（在仓库的 Actions 标签页查看）

**Q：想换主题颜色？**
A：修改 `global.css` 里的 `--primary` 变量值
